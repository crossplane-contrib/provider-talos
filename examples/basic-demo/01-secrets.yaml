# Basic Demo: Step 1 - Generate machine secrets
apiVersion: machine.talos.crossplane.io/v1alpha1
kind: Secrets
metadata:
  name: demo-secrets
spec:
  forProvider:
    # Secrets are generated automatically - no input needed
  providerConfigRef:
    name: default